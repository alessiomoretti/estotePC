CREATE TABLE measures( id INTEGER PRIMARY KEY NOT NULL,
                       tempA             REAL NOT NULL,
                       tempB             REAL NOT NULL,
                       tempC             REAL NOT NULL,
                       moistA            REAL NOT NULL,
                       moistB            REAL NOT NULL,
                       moistC            REAL NOT NULL );

INSERT INTO measures VALUES(1, 0, 0, 0, 0, 0, 0);
